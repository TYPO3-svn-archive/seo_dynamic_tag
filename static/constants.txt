plugin.tx_seodynamictag {

  # cat=Seo Dynamic Tag - Canonical*//100;   type=string;        label= Host: Host, if it differs in the host of the frontend
  canonical.host              = 
  # cat=Seo Dynamic Tag - Canonical*//200;   type=string;        label= Allowed parameter:Allowed URL parameter for the list view. An empty value is recommended!
  canonical.list.additionalParams  = 
  # cat=Seo Dynamic Tag - Canonical*//201;   type=string;        label= Allowed parameter*:*Obligate! Allowed URL parameter for the single view
  canonical.single.additionalParams  = &table[field]={GP:table|field}

  # cat=Seo Dynamic Tag - Conditions*//100;   type=string;        label= List view begin*:*Obligate! Global condition beginning for the list view. Without outer square brackets (!). Replace 'xxx' with the id of the page with your plugin for the list view. 
  condition.list.begin    = globalVar = TSFE:id = xxx
  # cat=Seo Dynamic Tag - Conditions*//101;   type=string;        label= List view end*:*Obligate! Global condition ending. Without outer square brackets (!). Usually: global
  condition.list.end      = global
  # cat=Seo Dynamic Tag - Conditions*//200;   type=string;        label= Single view begin*:*Obligate! Global condition beginning for the single view. Without outer square brackets (!). Replace 'xxx' with the id of the page with your plugin for the single view.
  condition.single.begin  = globalVar = GP:table|field > 0] && [globalVar = TSFE:id = xxx
  # cat=Seo Dynamic Tag - Conditions*//201;   type=string;        label= Single view end*:*Obligate! Global condition ending. Without outer square brackets (!). Usually: global
  condition.single.end    = global

  # cat=Seo Dynamic Tag - Debugging/enable/200;   type=boolean;   label= author: If enabled, you will get a report in the frontend for the meta tag author
  debugging.author      = 0
  # cat=Seo Dynamic Tag - Debugging/enable/201;   type=boolean;   label= canonical tag: If enabled, you will get a report in the frontend for the canonical tag
  debugging.canonical   = 0
  # cat=Seo Dynamic Tag - Debugging/enable/202;   type=boolean;   label= description: If enabled, you will get a report in the frontend for the meta tag description
  debugging.description = 0
  # cat=Seo Dynamic Tag - Debugging/enable/203;   type=boolean;   label= keywords: If enabled, you will get a report in the frontend for the meta tag keywords
  debugging.keywords    = 0
  # cat=Seo Dynamic Tag - Debugging/enable/204;   type=boolean;   label= title: If enabled, you will get a report in the frontend for the title
  debugging.title       = 0

  # cat=Seo Dynamic Tag - Default values//100;   type=string;     label= Author: Default author, if current record doesn't contain any author.
  default.author  = TYPO3 SEO Dynamic Tag (seo_dynamic tag)
  # cat=Seo Dynamic Tag - Default values//101;   type=string;     label= Description: Default description, if current record doesn't contain any description.
  default.description  = You have a bug in your SEO Dynamic Tag configuration: description is empty!
  # cat=Seo Dynamic Tag - Default values//102;   type=string;     label= Keywords: Default keywords, if current record doesn't contain any keyword.
  default.keywords  = TYPO3, SEO, Dynamic Tag, Bug

  # cat=Seo Dynamic Tag - page object//100;   type=string;        label= Label*:*Obligate! The label of your page in your TypoSCript configuration. Usually: page
  page.tsProperty = page 
  # cat=Seo Dynamic Tag - page object//200;   type=int;           label= SEO element*:*Obligate! The seo_dynamic_tag element of your page object like page.49442. 49442 is the number of the issue on TYPO3 forge and should be unique.
  page.object = 49442

}