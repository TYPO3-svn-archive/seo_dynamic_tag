plugin.tx_seodynamictag_pi1 {
    // 49442
  page =
  page {
      // meta tag author, meta tag description, meta tag keywords, title 
    49442 = COA
    49442 {
        // canonical tag
      10 = USER
      10 {
        includeLibs = typo3conf/ext/seo_dynamic_tag/pi1/class.tx_seodynamictag_pi1.php
        userFunc    = tx_seodynamictag_pi1->main
        special     = canonical
        host        = {$plugin.tx_seodynamictag.canonical.host}
        path        = TEXT
        path {
          typolink {
            parameter {
              data = page:uid
            }
            additionalParams = &tx_ttnews[tt_news]={GP:tx_ttnews|tt_news}
            additionalParams {
              insertData = 1
            } 
            //addQueryString = 1
            //addQueryString {
            //  method  = GET
            //  exclude = L,id,cHash,no_cache,tx_ttnews%5BbackPid%5D,best
            //}
            useCacheHash = 1
            returnLast = url
          }
        }
        debug = {$plugin.tx_seodynamictag.debugging.canonical}
      }
        // meta tag author
      20 = USER
      20 {
        includeLibs = typo3conf/ext/seo_dynamic_tag/pi1/class.tx_seodynamictag_pi1.php
        userFunc    = tx_seodynamictag_pi1->main
        special     = register
        register    = author
        query {
          select    = `author`
          from      = `tt_news`
          where     = `uid` = $1 && `deleted` = 0 && `hidden` = 0
          var.1     = tx_ttnews[tt_news]
        }
        debug   = {$plugin.tx_seodynamictag.debugging.author}
      }
        // meta tag description
      30 = USER
      30 {
        includeLibs = typo3conf/ext/seo_dynamic_tag/pi1/class.tx_seodynamictag_pi1.php
        userFunc    = tx_seodynamictag_pi1->main
        special     = register
        register    = description
        query {
          select    = `bodytext`
          from      = `tt_news`
          where     = `uid` = $1 && `deleted` = 0 && `hidden` = 0
          var.1     = tx_ttnews[tt_news]
          maxLength = 200
        }
        debug = {$plugin.tx_seodynamictag.debugging.description}
      }
        // meta tag keywords
      40 = USER
      40 {
        includeLibs = typo3conf/ext/seo_dynamic_tag/pi1/class.tx_seodynamictag_pi1.php
        userFunc    = tx_seodynamictag_pi1->main
        special     = register
        register    = keywords
        query {
          select    = `keywords`
          from      = `tt_news`
          where     = `uid` = $1 && `deleted` = 0 && `hidden` = 0
          var.1     = tx_ttnews[tt_news]
          keywords  = 0
        }
        debug = {$plugin.tx_seodynamictag.debugging.keywords}
      }
        // title
      50 = USER
      50 {
        includeLibs = typo3conf/ext/seo_dynamic_tag/pi1/class.tx_seodynamictag_pi1.php
        userFunc    = tx_seodynamictag_pi1->main
        special     = title
        query {
          select  = CONCAT(`title`, ': ', `short`)
          from    = `tt_news`
          where   = `uid` = $1 && `deleted` = 0 && `hidden` = 0
          var.1   = tx_ttnews[tt_news]
        }
        debug = {$plugin.tx_seodynamictag.debugging.title}
      }
    }
  }
}

  // Global condition for the single view
[{$plugin.tx_seodynamictag.condition.single.begin}]
  {$plugin.tx_seodynamictag.page.tsProperty} {
    meta {
      author {
        field   >
        ifEmpty = {$plugin.tx_seodynamictag.default.author}
        data    = register:author
      }
      description {
        field   >
        ifEmpty = {$plugin.tx_seodynamictag.default.description}
        data    = register:description
      }
      keywords {
        field   >
        data    = register:keywords
        ifEmpty = {$plugin.tx_seodynamictag.default.keywords}
      }
    }
    {$plugin.tx_seodynamictag.page.object} < plugin.tx_seodynamictag_pi1.page.49442
  }
[{$plugin.tx_seodynamictag.condition.single.end}]