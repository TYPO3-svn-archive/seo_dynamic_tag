plugin.tx_seodynamictag {

  canonical {
    list {
        // Only the pointer parameter is allowed
      additionalParams  = &tx_ttnews[pointer]={GP:tx_ttnews|pointer}
        // I don't like the pointer in the URL
      additionalParams  = 
    }
    single {
        // Only the uid parameter is allowed
      additionalParams  = &tt_products[product]={GP:tt_products|product}
    }
  }

  condition {
    list {
        // Please replace xxx with the uid of the page with the news plugin for the list view
        // Please use the Constant Editor
      begin = globalVar = TSFE:id = xxx
    }
    single {
        // Please replace xxx with the uid of the page with the news plugin for the single view
        // Please use the Constant Editor
      begin = globalVar = GP:tt_products|product > 0] && [globalVar = TSFE:id = xxx
    }
  }

  database >
  # cat=Seo Dynamic Tag - Database*//100;   type=boolean;          label= Info*:*Obligate! Database values are configured in the setup directly!
  database.dummy = 

}